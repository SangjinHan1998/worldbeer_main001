<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.worldbeer_main001">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Worldbeer_main001">
        <activity android:name=".NewsButton005"></activity>
        <activity android:name=".NewsButton004" />
        <activity android:name=".NewsButton003" />
        <activity android:name=".NewsButton002" />
        <activity android:name=".NewsButton001" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".worldbeer_menu" />
        <activity android:name=".worldbeer_news" />
        <activity android:name=".worldbeer_map" />
        <activity android:name=".worldbeer_member" />
    </application>

</manifest>